services:
  - type: web
    name: turnstile-solver
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python -m patchright install chromium
      python -m patchright install-deps chromium
    startCommand: python api_solver.py --browser_type chromium --host 0.0.0.0 --port 10000 --proxy
